<project name="jem-enterprise-properties" basedir=".">
	<!-- 
	Creates all properties used inside the build for JEM junit project
	Many of these properties are based on the common ones 
	-->
	<!-- import common properties -->
	<import file="../JEM/build-common-properties.xml" />
	<!-- properties for JEM Junit project -->
	<property name="jem.junit.jar.file" value="jem-junit.jar" />
	<property name="jem.junit.jar.file.location" value="${jem.junit.library.folder}/${jem.junit.jar.file}" />
</project>