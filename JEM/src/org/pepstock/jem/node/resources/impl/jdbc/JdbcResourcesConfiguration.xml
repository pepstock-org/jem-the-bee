<resource-template>
	
		<section name="Common Properties">
		
			<text-field key="driverClassName" 
						label="JDBC Driver" 
						description="The fully qualified Java class name of the JDBC driver to be used" 
						mandatory="true" defaultValue="" regExValidator=".*" />
			
			<text-field key="url" 
						label="URL" 
						description="The database url of the form jdbc:subprotocol:subname" 
						mandatory="true" defaultValue="" regExValidator=".*" />

			<text-field key="userid" 
						label="User ID" 
						description="The database user on whose behalf the connection is being made" 
						mandatory="true" defaultValue="" regExValidator=".*" />

			<password-field key="password" 
						label="Password" 
						description="The user's password" 
						mandatory="true" defaultValue="" regExValidator=".*" />
						
			<check-box-field key="defaultAutoCommit"
  				label="Auto Commit"
  				description="If a connection is in auto-commit mode, then all its SQL statements will be executed and committed as individual transactions"
  				mandatory="false" />
			
			<check-box-field key="defaultReadOnly"
  				label="Read Only"
  				description="If connections are readOnly by default"
  				mandatory="false" />
		
		</section>
		<section name="Connection Properties" propertiesEditor="true">
		</section>		
		
</resource-template>
